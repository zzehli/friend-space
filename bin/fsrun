#!/bin/bash

#call fsdb create if no database
#set FLASK_ENV and FLASK_APP
#run the server

set -x

FILE =var/friendspace.sqlite3

export FLASK_ENV=development
export FLASK_APP=friendspace
flask run --host 0.0.0.0 --port 8000